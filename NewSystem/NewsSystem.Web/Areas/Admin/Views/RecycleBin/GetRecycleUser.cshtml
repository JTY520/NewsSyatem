@model  IEnumerable<NewsSystem.Web.Areas.Admin.Models.UserViewModel>


@{
    ViewBag.Title = "GetRecycleUser";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
<script src="~/Scripts/jquery-1.12.4.js"></script>
<script src="~/Scripts/jquery.dataTables.min.js"></script>
<link href="~/Content/jquery.dataTables.min.css" rel="stylesheet" />

<div class="getnew">
    <strong>所有用户</strong>
    <div class="add-icon"><a href="/Admin/UserManage/AddUser">添加用户</a></div>
</div>
<hr />
<div class="getnews">
    <script src="~/Scripts/table.js"></script>

    <table id="example" class="getnews">
        <thead class="one-get-th">
            <tr>
                <th>帐号</th>
                <th>昵称</th>
                <th>操作</th>
            </tr>
        </thead>
        @if (Model != null)
        {
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.UserAccout</td>
                        <td>@item.NickName</td>
                       <td>
                           <a href="/Admin/RecycleBin/UserRecover?account=@item.UserAccout">还原</a>
                       </td>
                    </tr>
                }
            </tbody>
        }
        else
        {
            <tbody class="getnew">暂无数据</tbody>

        }
    </table>

</div>

