@model  IEnumerable<NewsSystem.Web.Areas.Admin.Models.NewsViewModel>



@{
    ViewBag.Title = "GetRecycleNews";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
<script src="~/Scripts/jquery-1.12.4.js"></script>
<script src="~/Scripts/jquery.dataTables.min.js"></script>
<link href="~/Content/jquery.dataTables.min.css" rel="stylesheet" />

<div class="getnews">
    <script src="~/Scripts/table.js"></script>
    <div class="getnew">
        <strong>已回收新闻</strong>
       
    </div>
    <hr />
    <table id="example" class="getnews">
        <thead class="one-get-th">
            <tr>
                <th>标题</th>
                <th>作者</th>
                <th>类型</th>
                <th>类型</th>
                <th>操作</th>
            </tr>
        </thead>
        @if (Model.Count() != 0)
        {
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td><a href="/Admin/NewsManage/GetOneNews?id=@item.Id">@item.Title</a></td>
                        <td>@item.Writer</td>
                        <td>@item.FirstType</td>
                        <td>@item.SecondType</td>
                        <td>
                            <a href="/Admin/RecycleBin/NewsRecover?id=@item.Id">还原</a>
                        </td>
                    </tr>
                }
            </tbody>
        }
        else
        {
            <tbody>暂无数据</tbody>
        }
    </table>
</div>